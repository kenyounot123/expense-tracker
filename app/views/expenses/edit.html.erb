<% content_for :title, "Edit #{@expense.income? ? "Income" : "Expense"}" %>

<%= content_for :header do %>
  <%= render "shared/header", title: "Edit #{@expense.income? ? "Income" : "Expense"}", back_path: expense_path(@expense) %>
<% end %>

<%= render "form", expense: @expense %>
