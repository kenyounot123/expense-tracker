<% content_for :title, "Edit Expense" %>

<%= content_for :header do %>
  <%= render "shared/header", title: "Edit Expense", back_path: expense_path(@expense) %>
<% end %>

<%= render "form", expense: @expense %>
